const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: 300,
    disponible: true,
}
console.log(Object.keys(producto)); // Muestra las llaves del objeto
console.log(Object.values(producto)); // Muestra los valores del objeto
console.log(Object.entries(producto)); // Muestra las llaves y valores del objeto