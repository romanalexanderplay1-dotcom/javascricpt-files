const booleana1 = true;
const booleana2 = false;

if (booleana1) {
    console.log('si es true');
} else {
    console.log('no es true');
}
console.log(booleana1 ? 'es true' : 'no es true');

const numero1 = 5;
const numero2 = 10;

if (numero1 > numero2) {
    console.log(`${numero1} es mayor que ${numero2}`);
} else {
    console.log(`No, ${numero2} es mayor que ${numero1}`);
}

console.log(numero1 > numero2 ? `${numero1} es mayor que ${numero2}` : `${numero2} es mayor que ${numero1}`);

const venezolano1 = 18;
 if (venezolano1 >= 18) {
    console.log(`Puede votar en las elecciones, porque su edad es ${venezolano1}`);
 } else {
    console.log(`No puede votar en las elecciones, porque su edad es ${venezolano1}`);
 }

console.log(venezolano1 >= 18 ? `Puede votar en las elecciones, porque su edad es ${venezolano1}` : `No puede votar en las elecciones, porque su edad es ${venezolano1}`);

const venezolano2 = 16;
    if (venezolano2 >= 18) {   
        console.log(`Puede votar en las elecciones, porque su edad es ${venezolano2}`);
    } else {
        console.log(`No puede votar en las elecciones porque su edad es ${venezolano2}`);
    }   
console.log(venezolano2 >= 18 ? `Puede votar en las elecciones, porque su edad es ${venezolano2}` : `No puede votar en las elecciones, porque su edad es ${venezolano2}`);

